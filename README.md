# Transcript Taxonomy Classifier (Gemini + Streamlit)

Streamlit app to classify Japanese transcripts into a fixed taxonomy using Gemini.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
